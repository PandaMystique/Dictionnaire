# Dictionnaire de Philosophie — APK Android

## Prérequis
- Node.js >= 18, Android Studio / SDK, Java JDK 17+

## Build
```bash
chmod +x build.sh
./build.sh          # APK debug
./build.sh release  # APK release
```
